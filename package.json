{
  "private": true,
  "scripts": {
    "start": "npm run build -- --watch",
    "prebuild": "mkdir -p build",
    "build": "ecmarkup --load-biblio @tc39/ecma262-biblio --verbose spec.emu build/index.html --lint-spec",
    "build:strict": "npm run build -- --strict"
  },
  "devDependencies": {
    "@tc39/ecma262-biblio": "^2.1.3023",
    "ecmarkup": "^23.0.2"
  },
  "engines": {
    "node": ">= 18"
  }
}
